"""Reusable modules for the Guess-the-Neuron Phase-1 pipeline.

Modules are added lazily, as the notebook that first needs them is built.
The current contents are deliberately minimal — see `WORKFLOW.md` for the
project's binding methodological constraints.
"""
