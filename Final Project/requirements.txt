# Core scientific stack
numpy>=1.24
pandas>=2.0
scipy>=1.11
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.13

# I/O
h5py>=3.9
pyarrow>=14.0          # parquet engine

# MICrONS dataset utilities
microns-datacleaner    # structural CSV downloader; used in src/data/loaders.py

# Notebooks
jupyter>=1.0
ipykernel>=6.25

# Phase-2 CNN dependencies (used later — kept here so a single install covers everything)
torch>=2.1

# Optional but useful
tqdm>=4.66
statsmodels>=0.14
