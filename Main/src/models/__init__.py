"""Model-architecture modules. Sklearn-compatible models live inline in
notebooks; only model classes that need their own training loop (PyTorch
set / temporal models) are factored out into modules here.
"""
